# PopX UI (Vite + React)

Quick scaffold that implements four screens (Welcome, Login, Register, Account) and responsive CSS to match provided designs.

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173
